'use strict';
module.exports = function (grunt) {
// 1.) configuration
  grunt.initConfig({
// ...
  });
// 2.) Load plug-ins and tasks
  grunt.loadNpmTasks('...');
// 3.) Define tasks
  grunt.registerTask('default', [ '...' ]);
};